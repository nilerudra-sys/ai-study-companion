{
  "plugins": [
    {
      "name": "python",
      "options": {
        "version": "3.12"
      }
    }
  ],
  "deploy": {
    "buildCommand": "pip install -r requirements.txt",
    "startCommand": "uvicorn app:app --host 0.0.0.0 --port $PORT"
  }
}
